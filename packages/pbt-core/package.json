{
  "name": "pbt-core",
  "version": "0.0.0",
  "license": "MIT",
  "main": "./lib/cjs/index.js",
  "module": "./lib/esm/index.js",
  "types": "./lib/esm/index.d.ts",
  "files": [
    "lib/"
  ],
  "dependencies": {
    "ix": "^4.0.0",
    "pure-rand": "^3.1.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2"
  },
  "scripts": {
    "build": "tsc --project tsconfig.esm.json && tsc --project tsconfig.cjs.json && ts-node --project scripts/tsconfig.json scripts/fixTypeScriptDefinitions",
    "build:watch": "tsc --project tsconfig.json --watch --preserveWatchOutput",
    "build:clean": "rimraf lib"
  }
}
