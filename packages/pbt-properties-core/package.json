{
  "name": "pbt-properties-core",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "files": [
    "lib/"
  ],
  "types": "./lib/index.d.ts",
  "dependencies": {
    "ix": "^3.0.2",
    "pbt-generator-core": "0.0.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "fast-check": "^2.2.0",
    "jest": "^26.4.1",
    "ts-jest": "^26.2.0",
    "typescript": "^4.0.2"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "build:watch": "tsc --project tsconfig.json --watch --preserveWatchOutput",
    "test": "jest --coverage",
    "test:watch": "jest --watchAll"
  }
}
