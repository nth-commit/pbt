{
  "name": "pbt-properties",
  "version": "0.0.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/nth-commit/pbt",
    "directory": "packages/pbt-properties"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "main": "./lib/cjs/index.js",
  "module": "./lib/esm/index.js",
  "types": "./lib/esm/index.d.ts",
  "files": [
    "lib/"
  ],
  "dependencies": {
    "ix": "^4.0.0",
    "pbt-core": "0.0.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "cross-env": "^7.0.2",
    "fast-check": "^2.2.0",
    "jest": "^26.4.1",
    "pbt-gen": "0.0.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.2"
  },
  "scripts": {
    "build": "tsc --project tsconfig.esm.json && tsc --project tsconfig.cjs.json && ts-node --project scripts/tsconfig.json scripts/fixTypeScriptDefinitions",
    "build:watch": "tsc --project tsconfig.json --watch --preserveWatchOutput",
    "build:clean": "rimraf lib",
    "build:test": "tsc --project test/tsconfig.json",
    "test": "jest --coverage --no-cache",
    "test:watch": "cross-env fastCheckRuns=10 jest --watchAll --no-cache",
    "pack": "npm pack",
    "sandbox": "ts-node --project scripts/tsconfig.json scripts/sandbox"
  }
}
